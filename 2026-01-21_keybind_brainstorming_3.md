- chords of N keys (ordered/unordered/mixed)
- sequences (ordered/unordered/mixed)



binary operators: ordered sequence, unordered sequence, ordered chord, unordered chord

associativity only between same operation
commutativity only for unordered operations


Z = prefix key "prefix:name"
X = "held:name" while held




folder ("prefix:name" OR "held:name")
